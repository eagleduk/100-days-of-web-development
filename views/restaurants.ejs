<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('includes/head'); -%>
        <link rel="stylesheet" href="styles/restaurants.css" />
</head>

<body>
    <%- include('includes/header'); -%>
        <%- include('includes/sidebar'); -%>
            <main>
                <h1>Recommended restaurants</h1>
                <% if(numberOfRestaurants===0) { %>
                    <p>
                        No have restaurants..
                    </p>
                    <% } else { %>
                        <p>Find your next favorite restaurants with help of our other users!</p>
                        <p>We found <%= numberOfRestaurants %> restaurants.</p>

                        <form action="" method="GET">
                            <button class="btn" name="order" value="<%= order %>">order</button>
                        </form>

                        <ul id="restaurants-list">
                            <% restaurants.forEach(function(restaurant){ %>
                                <%- include('includes/items/restaurant-item', {restaurant: restaurant}); -%>
                                    <% }); %>
                        </ul>
                        <% } %>

            </main>
</body>

</html>