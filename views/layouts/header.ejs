<header>
    <a href="/">
        WDE
    </a>
    <nav>
        <ul>
            <% if(!locals.isLogin) { %>
                <li>
                    <a href="/login">login</a>
                </li>
                <li>
                    <a href="/signup">signup</a>
                </li>
                <% } else if(locals.isLogin) { %>
                    <li>
                        <form action="/logout" method="POST">
                            <input type="hidden" name="_csrf" id="_csrf" value="<%= locals.csrf %>">
                            <button>
                                logout
                            </button>
                        </form>
                    </li>
                    <%} %>
        </ul>
    </nav>
</header>