<%- include("../meta/meta", {title: "View Product" }) %>
    <link rel="stylesheet" href="/static/styles/products.css">

    <script src="/static/scripts/product.script.js" defer></script>
    </head>

    <body>
        <%- include("../layouts/header") %>
            <main class="proudct-view-main">

                <header class="product-view-header">
                    <img src="/images/<%= product.filename %>" alt="<%= product.title %> ">
                    <div>
                        <h1>
                            <%= product?.title %>
                        </h1>

                        <h4>
                            ₩<%= product?.price %>
                        </h4>

                        <button class="btn" id="addCart" data-id="<%= product._id%>" data-csrf="<%=locals.csrf%>">Add
                            Cart
                        </button>
                    </div>
                </header>

                <p>
                    <%= product?.description %>
                </p>

            </main>
            <%- include("../layouts/footer") %>